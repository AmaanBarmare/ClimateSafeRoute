# ClimateSafe Route

A pedestrian navigation tool that routes people away from urban heat islands and flood zones — not just the shortest path.

NYC's extreme heat kills more people every year than hurricanes and floods combined, yet every navigation app ignores it. A 5-minute detour through a tree-lined street can mean a significant difference in heat exposure. ClimateSafe Route surfaces that information so pedestrians can make informed decisions about how they move through the city.

---

## How It Works

Enter an origin and destination anywhere in NYC. The app computes two routes simultaneously:

- **Standard route** — the shortest path by distance, identical to what Google Maps would show
- **Climate-smart route** — a path that minimizes exposure to heat-vulnerable areas, flood zones, and unshaded streets

Both routes appear on the same interactive map. A side panel shows the tradeoff statistics and a plain English explanation generated by an LLM (OpenAI `gpt-5-mini`) — something like *"This route adds 6 minutes but passes through 34% tree canopy versus 8%, and avoids two FEMA flood-risk zones near the waterfront."*

---

## Demo

![ClimateSafe Route demo](docs/demo.gif)

---

## Tech Stack

| Layer | Technology |
|---|---|
| Frontend | Next.js 14 (App Router), Tailwind CSS |
| Map | react-map-gl, Mapbox GL JS |
| Backend | FastAPI (Python 3.11) |
| Routing | OSMnx, NetworkX, GeoPandas |
| Database | PostgreSQL 15 + PostGIS 3 |
| LLM | OpenAI API (`gpt-5-mini`) |
| Frontend deploy | Vercel |
| Backend deploy | Railway |

---

## Data Sources

All climate data is sourced from free public datasets and pre-processed into PostGIS before the app runs. No live data fetching happens at request time.

| Dataset | Source | Coverage |
|---|---|---|
| Heat Vulnerability Index | NYC Open Data | NTA-level heat risk score |
| Tree Canopy Coverage | NYC Open Data | Block-level canopy percentage |
| FEMA Flood Zones | NYC Open Data | Flood risk polygon designations |
| Street Network | OpenStreetMap via OSMnx | Full NYC pedestrian street graph |

---

## Project Structure

```
climatesafe-route/
├── frontend/          # Next.js 14 app
│   ├── app/           # App Router pages
│   ├── components/    # React components
│   └── lib/           # API client + TypeScript types
├── backend/           # FastAPI app
│   ├── app/           # Application code
│   │   ├── routers/   # API endpoint handlers
│   │   ├── services/  # Geocoding, routing, LLM logic
│   │   ├── db/        # Database connection + models
│   │   └── schemas/   # Pydantic request/response models
│   └── scripts/       # One-time data ingestion scripts
├── docker-compose.yml # Local dev orchestration
└── CLAUDE.md          # Full technical specification for AI-assisted development
```

---

## Local Development

### Prerequisites

- Node.js 20+
- Python 3.11+
- Docker and Docker Compose
- A Mapbox account (free tier is sufficient) — get a token at https://account.mapbox.com
- An OpenAI API key — get one at https://platform.openai.com/api-keys

### 1. Clone the repository

```bash
git clone https://github.com/yourusername/climatesafe-route.git
cd climatesafe-route
```

### 2. Set up environment variables

```bash
# Frontend
cp frontend/.env.local.example frontend/.env.local
# Edit frontend/.env.local and add your Mapbox token

# Backend
cp backend/.env.example backend/.env
# Edit backend/.env and add your OpenAI API key and database URL
```

### 3. Start the database

```bash
docker-compose up -d
```

This starts a PostgreSQL instance with PostGIS enabled on port 5432.

### 4. Download the raw data files

Download the following datasets and place them in `backend/data/`:

- **Heat Vulnerability Index:** https://data.cityofnewyork.us/Health/Heat-Vulnerability-Index-NTA/2jr8-rugk → save as `hvi.geojson`
- **Tree Canopy:** https://data.cityofnewyork.us/Environment/Tree-Canopy/by9k-vhck → save as `canopy.geojson`
- **FEMA Flood Zones:** https://data.cityofnewyork.us/Environment/Floodplain/8qam-4vqv → save as `flood.geojson`

### 5. Run data ingestion (one time only)

```bash
cd backend
pip install -r requirements.txt

python scripts/ingest_hvi.py
python scripts/ingest_canopy.py
python scripts/ingest_flood.py
python scripts/precompute_graph.py    # This takes 5–10 minutes on first run
```

The precompute step pulls the NYC street network, attaches climate scores to every street edge, and stores the result in PostGIS. This only needs to run once. Subsequent app starts load the scored graph from the database.

### 6. Start the backend

```bash
cd backend
uvicorn app.main:app --reload --port 8000
```

FastAPI will be available at http://localhost:8000. Interactive API docs at http://localhost:8000/docs.

### 7. Start the frontend

```bash
cd frontend
npm install
npm run dev
```

The app will be available at http://localhost:3000.

---

## API Reference

### POST /route

Computes two routes between an origin and destination and returns them with climate statistics and an LLM-generated explanation.

**Request**

```json
{
  "origin": "Times Square, New York, NY",
  "destination": "Brooklyn Bridge, New York, NY"
}
```

**Response**

```json
{
  "route_shortest": {
    "geojson": { "type": "Feature", "geometry": { "type": "LineString", "coordinates": [[...]] } },
    "distance_m": 4200,
    "duration_min": 52,
    "heat_score": 78,
    "flood_score": 12,
    "canopy_pct": 8
  },
  "route_climate": {
    "geojson": { "type": "Feature", "geometry": { "type": "LineString", "coordinates": [[...]] } },
    "distance_m": 4650,
    "duration_min": 58,
    "heat_score": 41,
    "flood_score": 3,
    "canopy_pct": 34
  },
  "explanation": "The climate-smart route adds 6 minutes but passes through 34% tree canopy versus 8%, significantly reducing heat exposure. It also avoids two FEMA flood-risk zones near the waterfront.",
  "origin_coords": [-73.9857, 40.7580],
  "destination_coords": [-73.9969, 40.7061]
}
```

**Error codes**

| Code | Meaning |
|---|---|
| 422 | Origin or destination address could not be resolved |
| 500 | No valid route found between the two points |

---

## Deployment

### Frontend — Vercel

1. Push the repository to GitHub
2. Import the project at https://vercel.com/new
3. Set the root directory to `frontend/`
4. Add environment variables:
   - `NEXT_PUBLIC_MAPBOX_TOKEN` — your Mapbox public token
   - `NEXT_PUBLIC_API_URL` — your Railway backend public URL (set after backend is deployed)
5. Deploy

### Backend — Railway

**Step 1 — Create a Railway project** at https://railway.app/new

**Step 2 — Add a PostgreSQL service**
- Click "Add Service" → "Database" → "PostgreSQL"
- Once provisioned, open the service shell and run:
  ```sql
  CREATE EXTENSION postgis;
  ```

**Step 3 — Add the backend service**
- Click "Add Service" → "GitHub Repo"
- Select your repository, set the root directory to `backend/`
- Railway will detect the Dockerfile automatically
- Add environment variables:
  - `DATABASE_URL` — copy from the PostgreSQL service's "Connect" tab
  - `OPENAI_API_KEY` — your OpenAI API key
  - `ENVIRONMENT` — `production`

**Step 4 — Run ingestion scripts**
- After the first successful deploy, open the backend service shell
- Run the ingestion scripts in order (same as local dev step 5)

**Step 5 — Update Vercel**
- Copy the Railway backend's public URL
- Update `NEXT_PUBLIC_API_URL` in Vercel environment variables
- Redeploy the frontend

---

## How the Climate Score Is Computed

Each street edge in the graph receives a composite climate score computed as:

```
climate_score = (hvi_normalized × 0.5) + (flood_normalized × 0.3) + ((1 - canopy_normalized) × 0.2)
```

All three inputs are normalized to a 0–1 scale before weighting. A higher score means a more climate-risky street. The routing algorithm minimizes this score when computing the climate-smart route, using it as the edge weight in Dijkstra's algorithm via NetworkX.

The weights (0.5, 0.3, 0.2) reflect the relative severity of each risk factor: heat vulnerability is the dominant concern given NYC's heat mortality data, flood risk is significant but geographically concentrated, and tree canopy is a moderating factor rather than a primary risk.

---

## Limitations

- Coverage is currently limited to Manhattan in the first build; Brooklyn is added once Manhattan works end-to-end. Expanding to all five boroughs requires more memory during the precompute step.
- Climate data is static — it reflects conditions at the time of the last data download, not real-time weather. On an actual hot day, heat risk is higher than the index indicates.
- The app routes pedestrians only. It does not account for cycling or transit.
- Geocoding uses Nominatim (OpenStreetMap), which occasionally struggles with informal NYC address formats. If an address fails, try adding "New York, NY" to the end.

---

## Challenges Faced

Notes on the real engineering problems that came up while building this — kept short for revision.

### 1. Stale data sources
**Problem:** All three NYC datasets named in the original spec (HVI by NTA, per-block tree canopy, FEMA flood zones) had been retired or renamed by the time I started building. Endpoints returned 404.
**Fix:** Researched the NYC Open Data portal and found current replacements with HTTP 200 verification before integrating. Flood data switched from FEMA AE/VE/X zones to the **FSHRI** (Flood Susceptibility & Hazard Rank Index) on a 1–5 scale, which I rescaled to a 0–100 risk score. Canopy switched from per-block polygons to the **Street Tree Census** aggregated as trees-per-km² per ZCTA, then percentile-ranked to a 0–100 canopy proxy.
**Takeaway:** External datasets rot. Verify endpoints before depending on them, and isolate dataset access in one ingestion script per source so swaps stay localized.

### 2. OSMnx 2.x breaking changes
**Problem:** The spec assumed OSMnx 1.9.x but pip installed 2.x. `nearest_nodes()` crashed with `KeyError: 'crs'` and an `ImportError` for scikit-learn.
**Fix:** Explicitly set `G.graph["crs"] = "EPSG:4326"` after rebuilding the graph from PostGIS, and added `scikit-learn` to requirements.txt.
**Takeaway:** Pin major versions of geo libraries — they're under active development and breaking changes are common.

### 3. PostGIS extension not available on default Railway Postgres
**Problem:** Railway's default Postgres template doesn't include the PostGIS extension, and the platform doesn't allow installing extensions on managed databases. `CREATE EXTENSION postgis` failed.
**Fix:** Deployed the dedicated **PostGIS community template** instead of the default Postgres template.
**Takeaway:** When using managed databases, check extension support before committing — you can't always `CREATE EXTENSION` your way out.

### 4. `postgres://` vs `postgresql://` URL scheme
**Problem:** Railway's PostGIS template hands out connection strings starting with `postgres://`, but SQLAlchemy 2.x only accepts `postgresql://`. The backend crashed on startup with `Could not parse SQLAlchemy URL`.
**Fix:** Added a normalization step everywhere `DATABASE_URL` is read (6 files): `if url.startswith("postgres://"): url = url.replace("postgres://", "postgresql://", 1)`.
**Takeaway:** Connection string formats vary across providers and library versions. Normalize at the boundary, not at the call site.

### 5. Reasoning models consume the token budget silently
**Problem:** Switched the LLM to OpenAI `gpt-5-mini` and started getting empty responses. It's a reasoning model — it spends tokens on hidden chain-of-thought before producing visible output. With `max_tokens=200`, all 200 went to invisible reasoning and zero to the actual reply.
**Fix:** Set `reasoning_effort="minimal"` and bumped `max_completion_tokens` to 600. Added a deterministic template fallback for the rare case where the LLM still returns empty.
**Takeaway:** Reasoning models have different token economics than chat models. When debugging short outputs, inspect `usage.completion_tokens_details` to see how many tokens went to reasoning vs. visible output.

### 6. Same-ZCTA routes return identical paths
**Problem:** For short trips that stay within one ZCTA (NYC's modified ZIP code area), the climate multiplier is constant across every candidate edge, so Dijkstra returns the same path for both `weight='length'` and `weight='climate_score'`. The shortest and climate-smart routes look identical to the user.
**Why it happens:** The climate score is a per-ZCTA polygon score joined to street edges by spatial intersection. Inside a single polygon, every edge gets the same multiplier — so path ordering depends only on length.
**Fix:** Documented as a known resolution limit, and verified divergence works on longer cross-ZCTA trips (e.g., Battery Park → Dyckman, Grand Central → High Line).
**Takeaway:** Spatial joins inherit the resolution of the coarser dataset. Routing quality is bounded by climate-data granularity — moving to per-block-group data would fix it.

### 7. Memory cost of the in-memory graph (Brooklyn expansion)
**Problem:** Adding Brooklyn tripled the graph from ~115k to ~332k edges. FastAPI loads the whole graph at startup via `@lru_cache`, so that memory cost is paid 24/7, not per request — which directly affects hosting cost on a memory-priced platform like Railway.
**Fix:** Re-precomputed the graph against the production database, redeployed the backend so it reloaded the larger graph, and verified RAM stayed within limits.
**Takeaway:** Hot-loaded data structures trade RAM for latency. On per-resource-priced hosts, that floor cost should be in the design conversation early — not discovered after the deploy.

---

## Contributing

Pull requests are welcome. For significant changes, open an issue first to discuss what you'd like to change.

1. Fork the repository
2. Create a feature branch: `git checkout -b feature/your-feature-name`
3. Commit your changes: `git commit -m 'add: brief description'`
4. Push to the branch: `git push origin feature/your-feature-name`
5. Open a pull request

---

## License

MIT
